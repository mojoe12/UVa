#include <iostream>
#include <string>
#include <cmath>
using namespace std;
int main(int argc, char *argv[]) {
	/*int k, m;
	while (cin >> k >> m) {
		cout << k << " " << m << " ";
		int hourk = 12, hourm = 12;
		double seck = 0, secm = 0;
		do {
			seck += 60 - k / 1440.0;
			if (seck >= 3600) {
				seck -= 3600;
				hourk++;
				if (hourk == 13) hourk = 1;
			}
			secm += 60 - m / 1440.0;
			if (secm >= 3600) {
				secm -= 3600;
				hourm++;
				if (hourm == 13) hourm = 1;					}
			cout << mink << " " << minm << " " << hourk << " " << hourm << endl;
		} while (hourk != hourm || seck != secm);
		
		if (hourm < 10) cout << '0';
		cout << to_string(hourm) << ':';
		int hour = round(secm/60);
		if (hour < 10) cout << '0';
		cout << to_string(hour) << endl;
	}*/
	// THIS IS A MATH PROBLEM NOT A TIME PROBLEM
}