#include <iostream>

using namespace std;
int main(int argc, char *argv[]) {
	int k;
	cin >> k;
	for (int c = 0; c < k; c++) {
		if (c > 0) cout << endl;
		int m, n;
		cin >> m >> n;
		// read in shitters
		//
		int x;
		cin >> x;
		for (int y = 0; y < x; y++) {
			string word;
			cin >> word;
			//CHECKYHUTTS
		}
	}
}